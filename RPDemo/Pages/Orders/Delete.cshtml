@page
@model RPDemo.Pages.Orders.DeleteModel
@{
    ViewData["Title"] = "Delete Order";
}

<h1>Delete</h1>

@if(@Model.Order == null)
{
    <h4>The requested order was not found.</h4>
}
else
{
    <div class="h4">
        Are you sure you want to delete the order you placed on @Model.Order.OrderDate?
    </div>
    <form method="post">
        <input asp-for="Id" value="@Model.Id" type="hidden" />
        <div class="form-group">
            <input type="submit" value="Delete Order" class="btn btn-danger mt-1" />
        </div>
    </form>
}